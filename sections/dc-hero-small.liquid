{{ 'dc-hero-small.css' | asset_url | stylesheet_tag }}

<div class="hero-small" style="background-color: {{ section.settings.background_color }}">
  <div class="hero-small-content flex-box justify-between align-center{% if section.settings.media_alignment == 'left' %} rev-cols{% endif %}">
    <div class="hero-small-text">
      <div class="container">{{ section.settings.title }}</div>
    </div>
    <div class="hero-small-img"><img src="{{ section.settings.image | img_url: 'x720' }}"></div>
  </div>
</div>

{% schema %}
  {
    "name": "DC Hero Small",
    "settings": [
		{
			"type": "color",
			"id": "background_color",
			"label": "Background Color"
		},
	  {
        "type": "radio",
        "id": "media_alignment",
        "label": "Media alignment",
        "options": [
          {
            "value": "left",
            "label": "Left"
          },
          {
            "value": "right",
            "label": "Right"
          }
        ],
        "default": "right"
      },
		{
			"type": "text",
			"id": "title",
			"label": "Title"
		},
		{
			"type": "image_picker",
			"id": "image",
			"label": "Image"
		}
	],
	"presets": [
		{
			"name": "DC Hero Small"
		}
	]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
